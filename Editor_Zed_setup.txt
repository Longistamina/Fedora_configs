###########################
## zed config .json file ##
###########################

// Zed settings
//
// For information on how to configure Zed, see the Zed
// documentation: https://zed.dev/docs/configuring-zed
//
// To see all of Zed's default settings without changing your
// custom settings, run `zed: open default settings` from the
// command palette (cmd-shift-p / ctrl-shift-p)
{
  /////////////////////////////////
  "auto_update": false,
  //////////////////////////////////////////
  "minimum_contrast_for_highlights": 40.0,
  "selection_highlight": true,
  "rounded_selection": true,
  "current_line_highlight": "all",
  "unnecessary_code_fade": 0.5,
  "ui_font_weight": 200.0,
  "buffer_font_weight": 200.0,
  "buffer_font_family": "MesloLGL Nerd Font",
  //////////////////////////////////////////
  "agent": {
    "sidebar_side": "right",
    "dock": "right",
    "default_model": {
      "provider": "ollama",
      "model": "gemma4:26b",
      "enable_thinking": true,
    },
    "favorite_models": [],
    "model_parameters": [],
  },
  //////////////////////////////////////////
  "show_edit_predictions": true,
  "edit_predictions": {
    "provider": "zed",
    "ollama": {
      "model": "deepseek-coder-v2:16b",
    },
  },
  /////////////////////////////////////////
  "telemetry": {
    "diagnostics": false,
    "metrics": false,
  },
  /////////////////////////////////////////
  "diagnostics_max_severity": "warning",
  /////////////////////////////////////////
  "project_panel": {
    "dock": "left",
  },
  /////////////////////////////////////////
  "languages": {
    "Python": {
      "language_servers": ["ruff", "basedpyright"],
      "format_on_save": "off",
    },
    "Rust": {
      "format_on_save": "off",
    },
  },
  /////////////////////////////////////////
  "agent_servers": {
    "github-copilot-cli": {
      "type": "registry",
    },
  },
  /////////////////////////////////////////
  "icon_theme": "Material Icon Theme",
  "terminal": {
    "font_size": 16.0,
    "font_family": "MesloLGM Nerd Font",
    "detect_venv": "off",
  },
  "base_keymap": "VSCode",
  "ui_font_size": 16.0,
  "buffer_font_size": 15.0,
  "theme": {
    "mode": "dark",
    "light": "Ayu Light",
    "dark": "VSCode Modern Dark",
  },
}

######################
## Keymap shortcuts ##
######################

Zed -> Open Keymap -> task: rerun -> set as "Ctrl + R"

Zed -> Open Keymap -> repl: run -> set as "Shift + Enter"
Zed -> Open Keymap -> repl: restart -> set as "Ctrl + Shift + R"

Zed -> Open Keymap -> editor: show signature help -> set as "Shift + Tab"
Zed -> Open Keymap -> editor: backtab -> set as "Ctrl + Tab"

[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
    },
  },
  {
    "context": "Editor && vim_mode == insert",
    "bindings": {
      // "j k": "vim::NormalBefore"
    },
  },
  {
    "context": "Editor && jupyter",
    "bindings": {
      "shift-enter": "repl::RunInPlace",
      "ctrl-shift-i": "repl::Interrupt",
      "ctrl-shift-r": "repl::Restart",
    },
  },
  {
    "context": "Terminal",
    "bindings": {
      "ctrl-l": "terminal::Clear",
    },
  },
  {
    "context": "Terminal",
    "bindings": {
      "ctrl-enter": ["terminal::SendText", "\u001b[3;5~"],
    },
  },
  {
    "context": "Terminal",
    "bindings": {
      "ctrl-r": "task::Rerun"
    }
  },
  {
    "context": "Editor",
    "bindings": {
      "shift-tab": "editor::ShowSignatureHelp"
    }
  },
  {
    "context": "Editor",
    "bindings": {
      "ctrl-tab": "editor::Backtab"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "ctrl-r": [
        "task::Rerun",
        {
          "reevaluate_context": true
        }
      ]
    }
  },
  {
    "context": "((Editor && jupyter) && !ContextEditor)",
    "bindings": {
      "ctrl-shift-enter": null
    }
  },
  {
    "context": "((Editor && jupyter) && !ContextEditor)",
    "bindings": {
      "ctrl-alt-enter": null
    }
  }
]

####################################
## Register Python Jupyter kernel ##
####################################

# activate environment
source ~/<env_name>/bin/activate.fish # for uv, can also use ``source ~/dl/bin/activate.sh``
conda active <env_name> # for conda

# Install ipykernel
python3 -m ipykernel install --user --name=<env_name> # example: python3 -m ipykernel install --user --name=dl

~ = /home/username/ (/home/longdpt/)

#################################
## Setup debugger (For Python) ##
#################################

# Inside your project directory
mkdir .zed
touch .zed/debug.json

# pass these to debug.json
[
  {
    "adapter": "Debugpy",
    "label": "Python: Debug Current File",
    "request": "launch",
    "python": "/home/longdpt/dl/bin/python",
    "args": [],
    "cwd": "$ZED_WORKTREE_ROOT",
    "program": "$ZED_FILE",
    "subProcess": false
  }
]

# Note: should install debugpy (pip install debugpy)
# Most of the time, we should use ``Step Into`` and ``Step Over``

############################
## zed handle busy cursor ##
############################

gnome-text-editor ~/.local/share/applications/zed.desktop

#------#
[Desktop Entry]
Version=1.0
Type=Application
Name=Zed
GenericName=Text Editor
Comment=A high-performance, multiplayer code editor.
TryExec=/home/longdpt/.local/zed.app/bin/zed
StartupNotify=false
Exec=bash -l -c "/usr/bin/env -u WAYLAND_DISPLAY /home/longdpt/.local/zed.app/bin/zed %U"
Icon=/home/longdpt/.local/zed.app/share/icons/hicolor/512x512/apps/zed.png
Categories=Utility;TextEditor;Development;IDE;
Keywords=zed;
MimeType=text/plain;application/x-zerosize;x-scheme-handler/zed;
Actions=NewWorkspace;

[Desktop Action NewWorkspace]
Exec=bash -l -c "/usr/bin/env -u WAYLAND_DISPLAY /home/longdpt/.local/zed.app/bin/zed --new %U"
Name=Open a new workspace
#------#

update-desktop-database ~/.local/share/applications/

#-------#
Remove the old one

grep -ril "Name=Zed" \
  ~/.local/share/applications \
  /usr/share/applications \
  2>/dev/null
 
# -r: Recursive. It searches all files in the target directory and continues down through all subdirectories.
# -i: Ignore case. It makes the search case-insensitive (e.g., searching for "error" will also match "Error", "ERROR", or "ErRoR").
# -l: Files with matches. Instead of printing every individual line that contains the text, it only outputs the file path/name of the files where a match was found.
  
# Output:
/home/longdpt/.local/share/applications/zed.desktop
/home/longdpt/.local/share/applications/dev.zed.Zed.desktop

rm ~/.local/share/applications/dev.zed.Zed.desktop # the old one
update-desktop-database ~/.local/share/applications/
